{
  "buildCommand": "npm run build",
  "outputDirectory": ".",
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/mcp/:transport*",
      "destination": "/api/mcp/:transport*"
    }
  ]
}
