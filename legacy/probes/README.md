# Legacy MCP Probe Scripts

These root-level probe scripts were moved here during the V2 Dream governance work because they target the old workers.dev deployment shape and can mislead operators.

Use the current Makefile gates and the production Cloudflare Worker under `cloudflare-mcp/mcp-server/` for active validation.
