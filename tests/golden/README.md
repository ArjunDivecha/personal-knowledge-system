# Golden Expectations

This directory is reserved for expected outputs derived from fixture bundles.

Examples:

- expected `get_index` output
- expected `search` ordering for fixed queries
- expected Dream archive candidates
- expected promotion behavior after repeated retrieval

The goal is to compare behavior against known-good outputs, not just check for crashes.
