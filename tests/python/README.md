# Python Test Suite

This directory is reserved for `pytest`-based tests covering:

- parsing
- distillation
- backfill
- thin index generation
- staging seed validation

The current repo still relies mainly on operator scripts. This directory marks the intended home for true automated Python tests.
